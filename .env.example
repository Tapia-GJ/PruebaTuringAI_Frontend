# API Configuration
# Base URL for API calls. Leave empty for relative paths (proxied by Vite dev server)
# In production, set to your backend URL (e.g., https://api.turingai.com)
VITE_API_BASE=http://localhost:3000
